name: hlcv-ss23
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pillow
  - cudatoolkit=11.7  # CUDA toolkit for GPU support
  - pytorch=2.0.0
  - torchvision=0.15.0
  - pip
  - pip:
      - numpy
      - torch
      - torchvision
prefix: /home/hlcv_team004/miniconda3/envs/hlcv-ss23

